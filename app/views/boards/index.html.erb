<p id="notice"><%= notice %></p>

<h1>Board</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @boards.each do |board| %>
      <tr>
        <td><%= board.title %></td>
        <td><%= board.body %></td>
        <td><%= link_to 'Show', board %></td>
        <td><%= link_to 'Edit', edit_board_path(board) %></td>
        <!--このedit_board_pathの後にある（）ってなんだろう。引数？ずっと思っていたけどいまだにわからん-->
        <!--分かった！！！(board)に投稿のidみたいなのが入ってきて特定の投稿の編集画面に飛べるんや！！！！-->
        <td><%= link_to 'Destroy', board, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Board', new_board_path %>
